import React from 'react';

import { Container, Typography } from '@mui/material';

const Footer = () => {
  return (
    <div className="footer">
      <Container maxWidth="lg">
        <Typography variant="body1">stox</Typography>
      </Container>
    </div>
  );
};

export default Footer;
