import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2E2D4D',
    },
    secondary: {
      main: '#595794',
    },
    success: {
      main: '#84C318',
    },
    error: {
      main: '#E05260',
    },
  },
});

export default theme;
